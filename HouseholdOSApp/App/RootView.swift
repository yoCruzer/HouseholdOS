import SwiftUI

struct RootView: View {
    var body: some View {
        VStack(spacing: 8) {
            Text("HouseholdOS")
                .font(.title)
            Text("Foundation ready")
        }
        .padding()
    }
}
